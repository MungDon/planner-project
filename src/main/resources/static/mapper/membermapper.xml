<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.planner.mapper.MemberMapper">
	
	<insert id="createMember" parameterType="ReqOAuth2MemberAdd">
		INSERT INTO member(
			member_id,
			member_userid,
			member_birth,
			member_phone,
			member_gender,
			member_email,
			member_password,
			member_name,
			oauth_id,
			member_status,
			member_reg
		)	VALUES	(
			member_seq.NEXTVAL,
			'none',
			'2020-02-01',
			'none',
			'none',
			#{member_email},
			#{member_password},
			#{member_name},
			#{oauth_id},
			#{member_status},
			sysdate
		)
	</insert>
	<update id="fetchAdditionalUserInfo" parameterType="ReqOAuth2Signup">
		UPDATE member
		SET	
			member_userid = #{member_userid},
			member_birth = #{member_birth},
			member_phone = #{member_phone},
			member_gender= #{member_gender}	
		WHERE
			member_id = #{member_id}
	</update>
	
	<select id="findByOAuthID" resultType="ResOAuth2MemberLogin">
		SELECT *
		FROM member
		WHERE oauth_id = #{oauthId}
	</select>
</mapper>