<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.planner.mapper.MemberMapper">
	<insert id="createMember" parameterType="ReqOAuth2MemberAdd">
		INSERT INTO member(
			member_id,
			member_userid,
			member_birth,
			member_phone,
			member_gender,
			member_email,
			member_password,
			member_name,
			oauth_id,
			member_status,
			member_reg
		)	VALUES	(
			member_seq.NEXTVAL,
			'123',
			'2000-12-01',
			'010-1111-1111',
			'm',
			#{member_email},
			#{member_password},
			#{member_name},
			#{oauth_id},
			#{member_status},
			sysdate
		)
	</insert>
	<select id="findByEmail" resultType="ResOAuth2MemberLogin">
		SELECT *
		FROM member
		WHERE member_email = #{email}
	</select>
</mapper>